import React from "react";
import Form from "./components/form/form";
const App: React.FC = (props: any) => {
  return (
    <div className="App">
      <Form />
    </div>
  );
};

export default App;
